# Test profile configuration
# Activated with: --spring.profiles.active=test or SPRING_PROFILES_ACTIVE=test

# Disable authentication for test environment (no credentials needed)
auth.enabled=false

# Gemini API configuration for testing
gemini.api.key=YOUR_SECURE_API_KEY_HERE
